<%= form_for(kart_item) do |f| %>
  <% if kart_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kart_item.errors.count, "error") %> prohibited this kart_item from being saved:</h2>

      <ul>
      <% kart_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quantity %>
    <%= f.number_field :quantity %>
  </div>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.text_field :product_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
